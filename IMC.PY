# Entrada de dados. 

peso = float(input("Digite o seu peso(kg): "))

Altura = float(input("Sigite sua altura(m): "))

imc = peso / (Altura ** 2)

print(f"Seu IMC é: {imc:.2f}")

#  agora vamos enviar para o repositorio Remoto e mandar para o github...
# comando git add . ---> git commit -m ---> git push origen primeiro_programa





